# Core
numpy>=1.23
scipy>=1.9
pandas>=1.5
tabulate>=0.9        # pandas.DataFrame.to_markdown backend
scikit-learn>=1.2
PyYAML>=6.0
tqdm>=4.64

# Deep learning
torch>=2.0
torchvision>=0.15

# Experiment tracking / logging
tensorboard>=2.12
# wandb is optional; install with `pip install wandb`

# Visualization
matplotlib>=3.6
seaborn>=0.12
# umap-learn>=0.5     # optional: UMAP embeddings (t-SNE/PCA work without it)

# Statistics
scikit-posthocs>=0.7   # Nemenyi post-hoc (optional; only needed for that test)

# EEG / signal processing (optional dataset loaders)
mne>=1.3            # optional, used by some real-dataset loaders
